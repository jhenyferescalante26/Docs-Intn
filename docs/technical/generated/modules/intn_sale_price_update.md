<!-- GENERATED by scripts/generate-technical-docs.py — DO NOT EDIT -->

# intn_sale_price_update

Mass update product list prices via a two-step wizard

**Grupo:** `intn_sales` · **Versión:** 18.0.1.0.0 · **Categoría:** Sales

**Depende de:** `intn_product_organization`, `product`, `sale`

## Modelos

**Definidos:** `intn.price.history`, `intn.price.update.wizard`, `intn.price.update.wizard.line`, `product.template`

**Extendidos:** `product.product`

## Diagrama de relaciones

```{graphviz} ../diagrams/intn_sale_price_update.dot
```

### Campos de `intn.price.history`

| Campo | Tipo | Etiqueta | Req. | Detalle |
|---|---|---|---|---|
| `changed_at` | datetime | Changed At |  |  |
| `changed_by` | many2one | Changed By |  | → `res.users` |
| `currency_id` | many2one | Currency |  | → `res.currency` |
| `new_price` | float | New Price |  |  |
| `old_price` | float | Previous Price |  |  |
| `product_id` | many2one | Product | sí | → `product.template` |

### Campos de `intn.price.update.wizard`

| Campo | Tipo | Etiqueta | Req. | Detalle |
|---|---|---|---|---|
| `adjustment_type` | selection | Adjustment Type | sí | valores: increase, discount |
| `amount` | float | Amount (Gs.) |  |  |
| `category_ids` | many2many | Categories |  | → `product.category` |
| `currency_id` | many2one | Currency |  | → `res.currency` |
| `filter_mode` | selection | Filter Mode | sí | valores: all, category, manual |
| `line_ids` | one2many | Preview Lines |  | → `intn.price.update.wizard.line` |
| `method` | selection | Method | sí | valores: percentage, fixed |
| `product_count` | integer | Product Count |  | compute (no store) |
| `product_ids` | many2many | Products |  | → `product.template` |
| `state` | selection | State | sí | valores: draft, preview |
| `value` | float | Percentage (%) |  |  |

### Campos de `intn.price.update.wizard.line`

| Campo | Tipo | Etiqueta | Req. | Detalle |
|---|---|---|---|---|
| `currency_id` | many2one | Currency |  | → `res.currency`; related: `wizard_id.currency_id` |
| `current_price` | float | Current Price |  |  |
| `new_price` | float | New Price |  |  |
| `product_id` | many2one | Product | sí | → `product.template` |
| `wizard_id` | many2one | Wizard | sí | → `intn.price.update.wizard` |

### Campos de `product.product`

| Campo | Tipo | Etiqueta | Req. | Detalle |
|---|---|---|---|---|
| `price_history_count` | integer | Price Changes |  | related: `product_tmpl_id.price_history_count` |
| `price_history_ids` | one2many | Price History |  | → `intn.price.history`; related: `product_tmpl_id.price_history_ids` |

### Campos de `product.template`

| Campo | Tipo | Etiqueta | Req. | Detalle |
|---|---|---|---|---|
| `price_history_count` | integer | Price Changes |  | compute (no store) |
| `price_history_ids` | one2many | Price History |  | → `intn.price.history` |

## Vistas

| Vista | Tipo | Modelo | Hereda de |
|---|---|---|---|
| intn.price.history.list | list | `intn.price.history` |  |
| intn.price.history.search | search | `intn.price.history` |  |
| intn.price.update.wizard.form | form | `intn.price.update.wizard` |  |
| product.template.form.price.history | form | `product.template` | product.template.product.form |

## Menús y acciones

**Menús:**

- Sales/Products/Price Management
- Sales/Products/Price Management/Mass Price Update
- Sales/Products/Price Management/Price Change History

| Acción | Modelo | Vistas |
|---|---|---|
| Mass Price Update | `intn.price.update.wizard` | form |
| Price Change History | `intn.price.history` | list |

## Seguridad

**Grupos definidos:**

- Sales / Price History Viewer
- Sales / Price Update Manager

**Permisos de acceso (ACL):**

| Modelo | Grupo | Permisos |
|---|---|---|
| `intn.price.history` | Sales / Price History Viewer | `r---` |
| `intn.price.update.wizard` | User types / Internal User | `rwcd` |
| `intn.price.update.wizard.line` | User types / Internal User | `rwcd` |
